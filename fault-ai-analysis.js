(()=>{
const form=document.getElementById('requestForm');const photo=document.getElementById('faultPhoto');if(!form||!photo)return;
const box=document.createElement('div');box.id='faultAiAnalysis';box.className='fault-ai-analysis';box.innerHTML='<button type="button" id="analyzeFaultBtn">🤖 تحليل العطل بالصورة</button><p id="faultAiResult" class="muted">سيساعد التحليل على اقتراح نوع العطل والخدمة المناسبة. النتيجة تقديرية وليست تشخيصًا مؤكدًا.</p>';
photo.closest('label')?.appendChild(box);const result=box.querySelector('#faultAiResult');
box.querySelector('#analyzeFaultBtn').addEventListener('click',async()=>{const file=photo.files?.[0];if(!file){result.textContent='📸 أضف صورة للعطل أولًا.';return;}result.textContent='🔎 جارٍ تحليل الصورة...';const reader=new FileReader();reader.onload=async()=>{try{const r=await fetch('/api/analyze-fault',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:reader.result,language:localStorage.getItem('khedma_lang')||'ary'})});if(!r.ok)throw new Error('analysis unavailable');const d=await r.json();result.innerHTML=`🤖 <strong>${d.category||'غير محدد'}</strong><br>${d.summary||''}<br>🔧 الخدمة المقترحة: ${d.suggested_service||'مقدم خدمة مختص'}<br>⚠️ ${d.safety_note||'النتيجة تقديرية وليست تشخيصًا مؤكدًا.'}`;}catch(e){result.textContent='تعذر تحليل الصورة حاليًا. حاول مرة أخرى لاحقًا.';}};reader.readAsDataURL(file);});
})();
