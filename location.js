(()=>{const input=document.getElementById('requestLocation');const mapEl=document.getElementById('locationMap');if(!input||!mapEl)return;let map=null,marker=null;const status=document.getElementById('locationStatus');const saved=JSON.parse(localStorage.getItem('khedma_location')||'null');function initMap(lat=33.5731,lon=-7.5898){if(map)return;map=L.map('locationMap').setView([lat,lon],13);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);marker=L.marker([lat,lon]).addTo(map).bindPopup('📍 موقعك').openPopup()}function setPos(lat,lon,label){initMap(lat,lon);map.setView([lat,lon],15);marker.setLatLng([lat,lon]);input.value=label;localStorage.setItem('khedma_location',JSON.stringify({latitude:lat,longitude:lon,label}));status.textContent='✓ تم تحديد موقعك'}if(saved?.latitude&&saved?.longitude){setPos(saved.latitude,saved.longitude,saved.label||`${saved.latitude}, ${saved.longitude}`)}else initMap();document.getElementById('useLocation').onclick=()=>{if(!navigator.geolocation){status.textContent='المتصفح ما كيدعمش تحديد الموقع';return}status.textContent='كنحددو موقعك...';navigator.geolocation.getCurrentPosition(async p=>{const{latitude,longitude}=p.coords;let label=`${latitude.toFixed(5)}, ${longitude.toFixed(5)}`;try{const r=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${latitude}&lon=${longitude}&accept-language=ar`);if(r.ok){const d=await r.json();label=d.display_name||label}}catch(e){}setPos(latitude,longitude,label)},()=>{status.textContent='سمح للموقع من فضلك باش نحددو بلاصتك'},{enableHighAccuracy:true,timeout:10000,maximumAge:300000})}})();